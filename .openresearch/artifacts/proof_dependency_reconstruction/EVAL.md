# Evaluator guide

Locate `proof_dependency_reconstruction` in `ORX_EVIDENCE`. Verify both source hashes, every proof predicate, the three rejected controls, `formal_proof_certificate_present: false`, and `BLOCKED` verdicts for Claims 1–4. The final independent checker must pass and reject tampering.

This is the third materially different route for the four LOW-confidence claims. It must not be presented as formal theorem verification. If confidence remains LOW, the next route is the mandatory assumption-satisfying falsification search.
