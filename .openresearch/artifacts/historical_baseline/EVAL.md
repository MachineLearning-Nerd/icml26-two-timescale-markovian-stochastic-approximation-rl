# Historical rejected baseline

The verifier is `reproduction/runner.py`. It exits nonzero if any historical numerical gate fails and prints the complete raw JSON between `ORX_EVIDENCE_BEGIN` and `ORX_EVIDENCE_END` for capture by `orx logs`.

Scientific verdict before stronger work: **BLOCKED**. The finite toy/proxy checks are not theorem proofs.
